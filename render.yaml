services:
  - type: web
    name: tariff-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn index:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0 